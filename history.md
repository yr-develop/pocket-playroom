# POCKET PLAYROOM 開発履歴

最終更新: 2026-09-25

## プロジェクトの目的

ブラウザで手軽に遊べる小規模ゲームを、ロビーから選択して起動できる形で継続的に追加していくプロジェクトとして開始した。

見た目は現代的でリッチなWebアプリへ寄せすぎず、少し昔の携帯ゲーム機を思わせる筐体、液晶画面、物理ボタン風の操作感を目標としている。現在の開発と動作確認はローカル環境だけで完結させ、外部サービスへの公開やデプロイは行っていない。将来的にはGitHub上へ配置し、静的URLから遊べる構成にできることも考慮している。

## 基本設計

- ルートの `index.html` をロビー兼ゲーム選択画面とする。
- 各ゲームは `games/<game-id>/` 以下へHTML、CSS、JavaScriptをまとめる。
- ロビーへ表示するゲームは `assets/js/games.js` のカタログで管理し、ロビー本体を大きく変更せず追加できるようにする。
- 筐体、画面、ボタン、色、基本タイポグラフィは `assets/css/base.css` で共有する。
- タッチ、画面内ボタン、キーボード操作を共存させる。
- 十字キー、SELECT、START、B、Aを共通の基本配置とする。
- 中央の補助ボタンはSELECTとSTARTの計2つだけとし、配置を維持したままHOLDやMENUなどへ役割を置き換える。
- 画面内操作とキーボード入力は `assets/js/controls.js` から `pocket-input` イベントへ変換し、ゲーム側の入力処理を共通化する。
- 効果音は `assets/js/soundboard.js` の共通サウンドプールに登録し、各ゲームが名前を指定して再生する。
- 音量設定はローカルストレージで共有し、ロビーからON/OFFできるようにする。
- 外部APIやサーバー処理に依存しない静的構成とする。
- `100svh`、セーフエリア、縦横レイアウト、320px程度の狭い画面を考慮する。
- OSの `prefers-reduced-motion` 設定を尊重する。

## 開発の経緯

### 1. ロビーと共通基盤

最初にゲームを追加し続けられるディレクトリ構成とロビーを作成した。ロビーにはカセット風のゲーム一覧、選択状態、ゲーム数表示、共通の携帯ゲーム機風コントローラーを用意した。

マウスだけに依存せず、タッチと十字キー・A/Bボタンの両方で操作できることを継続方針とした。ゲーム内の説明文にも「タップ」だけではなく、対応するボタン操作を併記することにした。

### 2. 第1ゲーム「ひとりババ抜き」

CPUと1対1で遊ぶ、ジョーカーを1枚使った簡易ババ抜きを追加した。同じ数字のペアは自動的に捨てられ、最後にジョーカーを持っていた側が負けとなる。

初期版は処理の進行が速く、プレイヤーが配札、カード取得、ペア成立、CPUの行動を認識しにくかった。このため各工程へ短い待ち時間とアニメーションを追加し、現在何が起きているかをステータス表示でも示す構成へ変更した。

その後、CPUがプレイヤーのカードを引いたあとにCPU側の手札順が維持され、ジョーカーの位置を記憶できる問題が判明した。対策として、CPUの各ターン終了時に見た目だけではなく手札のデータ配列自体を再シャッフルし、シャッフル演出を伴わせるよう修正した。これにより、前ターンの位置記憶だけで確定勝利できないようにした。

### 3. サウンド基盤

ゲームごとに個別の音源や発振処理を持たせるのではなく、ファミコン時代の容量節約のような共通規格を意識してサウンドボードを作成した。

短い8bit風の音色パターンを共通プールへ登録し、決定、移動、カード、シャッフル、成功、失敗などの用途名で参照する。音はゲーム体験を邪魔しない控えめな音量と長さにしている。ロビーの設定は各ゲームと共有される。

### 4. 第2ゲーム「スライドパズル」

数字が書かれたタイルを並べ替える、3×3・4×4・5×5対応のスライドパズルを追加した。

タイルのタップと十字キー操作の両方に対応し、経過時間と手数を記録する。途中で盤面サイズが変わって進行状況を失わないよう、開始後のサイズ変更にはRESET操作が必要となるよう整理した。PAUSEと、移動可能なタイルを短く光らせるHINTも追加した。

### 5. 操作系の統一とRESET安全機構

NEW GAMEやRESTARTを十字キーとA/Bの間へ通常ボタンとして置くと、誤操作で進行状況を失う危険があるため、操作配置を再設計した。

当初は特殊操作と長押しRESETも置ける設計だったが、現在は次の順序だけを基本としている。

1. 十字キー
2. SELECT
3. START
4. B / A

SELECTとSTARTの表示名は、ゲーム内容に合わせてSIZE、PAUSE、RULE、HOLD、MENUなどへ変更できる。専用の特殊操作ボタンとRESETボタンは後の統一で撤去した。

### 6. 第3ゲーム「絵合わせポーカー」

携帯ゲーム機のミニゲームとして遊べる絵合わせポーカーを追加した。参考画像から、上部の所持コインと配当、中央のカード列、下部の操作領域という画面構成とゲーム進行の考え方を参考にした。一方で、キャラクター画像や既存ゲームの素材は使用せず、太陽、星、ハート、7、ベリーを表す独自の記号カードで構成している。

初期版はソロゲームだったが、現在はCPUにも5枚を配る対戦ゲームである。変更したいカードを選んで1回交換し、CPUも重複絵柄を残す基本戦略で交換してからSHOW DOWNする。役は1ペア、2ペア、3カード、フルハウス、4カード、5カードを判定する。

操作はカードのタップ、または十字キーで選択してAで変更対象にする。対象がなければGO!、1枚以上あればCHANGEとなり、STARTまたは画面内ボタンで確定する。SELECTでBET変更、Bで変更対象を全解除する。

### 7. ロビー表示の改善と第4ゲーム「テトリス」

ロビーのカセット一覧を1列で連続して並べ、一覧内の縦スクロールバーを非表示にした。通常時の説明文は1行で省略し、マウスカーソル、キーボード選択、最初のタッチで選択されたカセットだけを少し拡大して全文表示する。タッチ操作では、1回目で内容を確認し、同じカセットの2回目のタッチで起動する。

第4ゲームとしてテトリスを追加した。7種類を1巡ずつ出す7バッグ方式、NEXT 3個、1ブロックにつき1回のHOLD、ゴースト表示、ソフト／ハードドロップ、左右回転、ロック待ち、ライン消去、レベルに応じた落下速度、スコアを実装した。T-Spin判定とボーナスは設けていない。

操作は左右で移動、上でハードドロップ、下でソフトドロップ、Aで右回転、Bで左回転、SELECTでHOLDとした。STARTはポーズメニューとして、コンティニュー、終了、リセットを十字キーとAで選べる。専用の特殊ボタンとRESETボタンは追加していない。

回転処理は現行Tetris Guidelineの標準として整理されているSRS（Super Rotation System）へ変更した。JLSTZはミノ中心、Iは格子交点を中心に回転し、JLSTZ共通とI専用の回転状態別ウォールキックを最大5位置まで順番に試す。Oは見た目の位置を変えない。T-Spinの得点判定は追加していない。

ライン消去はロック処理を再入不可にし、消去対象行を一括で除外して同数の空行を上部へ補充する方式へ変更した。消去待ちタイマーにはゲームセッションを関連付け、リセット後の古いタイマーが新しい盤面を書き換えないようにした。

画面内コントローラーはタッチ開始時点で入力を発行し、別々の指による十字キーとA・Bなどの同時タップを受け付ける。キーボード入力を初めて検知した場合だけ、筐体下部に矢印、Enter=A、Space=Bの対応を小さく表示する。

### 8. ワイド画面対応とローカル起動ファイル

当初は横型の920×760を基準にしたが、ゲーム内部がワイド向けレイアウトへ切り替わって崩れる問題が残ったため、この方針を廃止した。現在は画面サイズにかかわらず、ゲームボーイ風の430×800縦型筐体とスマートフォン向け内部レイアウトだけを使用する。表示領域へ収まらない場合は筐体全体を等倍縮小し、ワイド画面で余った左右・上下は暗い背景のデッドスペースとして残す。

Windowsでダブルクリックしてテストできる `localhost-start.bat` と `localhost-stop.bat` を追加した。起動ファイルはPythonの静的サーバーをバックグラウンドで開始してブラウザを開き、終了ファイルは記録したプロセスだけを停止する。

### 9. 共通UIとゲーム進行の再整理

ロビーは縦並びカセットを廃止し、選択中ゲームの説明を表示する横長カセット1枚と、その下の4列アイコングリッドへ変更した。十字キーやタップでアイコンを選ぶと中央説明が常に更新される。トップバーはSTEREO、スピーカー溝、SOUNDの3列幅を固定し、ON/OFFで文字数が変わっても位置が動かない。

全ゲームの中央操作部をSELECT・STARTの2ボタンだけへ統一し、特殊操作と専用RESETを撤去した。筐体、液晶、十字キー、中央ボタン、A/Bはゲームを切り替えても同じ領域を使う。スライドパズルのSIZEは最初の一手以降グレーアウトし、変更できないことを視覚化した。

ババ抜きは手札が15枚を超える間だけ9列の複数段表示にして全カードの数字と絵柄を読めるようにし、枚数が減った後は大きなカードを扇状に表示する。

### 10. 第5～第8ゲームの追加

第5ゲーム「本格ポーカー」を追加した。52枚デッキからCPUとプレイヤーへ5枚ずつ配り、双方が1回交換した後、SHOW DOWN直前にBETを調整できる。FOLD、役判定、同役時の比較、ポット、チップ、CPUのFOLD判断を実装した。最低ベット単位は3ラウンドごとに上昇する。

第6ゲーム「ポケットインベーダー」を追加した。左右移動、ショット、敵編隊の往復・下降、敵弾、残機、スコア、ハイスコア、ウェーブ進行、PAUSEに対応する。

第7ゲーム「ブロック崩し」を追加した。パドル位置による反射角度、ブロックの段別得点、残機、ステージ進行、スコア、ハイスコア、PAUSEに対応する。

第8ゲーム「マインスイーパー」を追加した。初手と周囲の安全保証、数字、空白マスの連鎖展開、フラグ、タイマー、9×9・10地雷と12×12・22地雷の切り替えに対応する。

### 11. 視認性改善、インベーダー修正、第9ゲーム

マインスイーパーは未開封マス、開封済みの空白マス、数字マスの背景色と罫線を分離し、数字を太くして色ごとのコントラストを高めた。

ポケットインベーダーに、敵弾と自弾で段階的に削れる4基の防御シールドを追加した。また、編隊が端を越えた座標のまま反転して毎フレーム下降していた不具合を修正し、端へ到達した時点で編隊全体を画面内へ補正してから1段だけ下降するようにした。

第9ゲーム「ポケットスネーク」を追加した。十字キーによる方向転換、AのBOOST、壁に当たるWALLモード、反対側へ抜けるWRAPモード、成長に伴うレベルと速度上昇、スコア、ハイスコア、PAUSEに対応する。

### 12. 全体監査と操作・表示の修正

9ゲーム追加後に全画面を再巡回したところ、430×800の筐体をCSSの `transform: scale()` だけで縮小していたため、見た目は小さくなってもレイアウト上の高さが800pxのまま残り、ブラウザの表示高が低い環境では下部コントローラーが切れる問題が判明した。筐体をビューポート中央へ固定配置し、中央基準で縮小する方式へ変更した。720px高の確認環境では上下8pxの余白内へ筐体全体が収まり、ページスクロールも発生しないことを確認した。

共通入力へ押下開始・終了イベントを追加した。これにより、ポケットインベーダーとブロック崩しでは、マウス・タッチで画面内十字キーを押している間もキーボードと同様に連続移動できる。通常のタップ操作や既存ゲームの単発入力は維持している。

本格ポーカーでは同じ役の比較方法を修正した。ストレートは最高札を比較し、A・2・3・4・5を5ハイとして扱う。フラッシュ、フォーカード、フルハウス、スリーカード、ツーペア、ワンペアも役を構成する札とキッカーを正しい優先順で比較するようにした。デッキの生成もランダム比較ソートからFisher–Yatesシャッフルへ変更した。

マインスイーパーのSTART/NEWは短押しで盤面が消えていたため、共通の3秒長押しと画面内カウントダウンを使用するよう修正した。短押しでは盤面を維持する。追加ゲームの案内文も、画面内ボタンのタップとキーボード・十字操作を併記する表現へ統一した。

修正後はロビーと9ゲームすべてで初期表示、筐体の収まり、主要コントロール、JavaScriptエラーの有無を確認した。ババ抜きのドロー、スライドパズルのHINT、両ポーカーの1ラウンド、テトリスのメニュー、インベーダーのショット、ブロック崩しの発射、マインスイーパーの初手と短押しNEW防止、スネークの開始までを実操作で確認した。

### 13. ロビー入力、JOKER表示、第10・第11ゲーム

ロビー内のゲームアイコンや中央カセットへフォーカスがあると、ブラウザ標準のボタン操作が優先されてEnter=A、Space=Bが発行されない問題を修正した。ロビーではフォーカス位置にかかわらずゲーム機入力を優先し、Enterで選択ゲームを起動、Spaceでサウンドを切り替える。

ひとりババ抜きのJOKERを通常のJACKから明確に区別できるよう、JOKERだけを黒地のカードに変更した。カード中央には `[JOKER]` を90度回転した縦向き表記として配置した。

第10ゲーム「ポケットランナー」を追加した。キャラクターが自動で走る横スクロール画面で、Aまたは上方向だけを使って障害物をジャンプする。進行に伴う速度上昇、障害物通過ボーナス、スコア、ハイスコア、PAUSEに対応する。

第11ゲーム「ビートパレード」を追加した。左・下・上・右の矢印とB・Aの6種類のノーツが上から判定ラインへ流れ、対応入力との時間差をBAD、OK、GOOD、NICE!の4段階で評価する。スコア、コンボ、ベストスコア、PAUSEに対応する。

### 14. localhost起動・終了ツールのキャッシュ対策

ルート上のファイルと4173番サーバーが返すファイルは一致していたが、従来のPython標準サーバーにはキャッシュ制御ヘッダーがなく、BATから同じ `http://localhost:4173/` を開いた場合にブラウザが以前のCSS・JavaScriptを再利用できる状態だった。また、PIDファイルに記録されたプロセスが存在するかだけで起動済みと判定しており、正しいプロジェクトフォルダを配信しているか確認できなかった。

`scripts/local-server.py` を追加し、すべてのレスポンスへ `no-store / no-cache` を設定した。HTMLを返す際にはソースを変更せず、参照するローカルCSS・JavaScriptへ各ファイルの更新時刻をバージョン文字列として付加する。これにより、更新後のアセットが確実に再取得される。

起動・終了PowerShellも専用ヘルスチェックへ対応させた。配信元の絶対パスと実際のサーバーPIDを確認してから再利用・終了し、別のサーバーが4173番ポートを使用している場合は停止せずエラーにする。IPv6の名前解決待ちを避けるため、内部ヘルスチェックは `127.0.0.1` を使用し、利用者向けURLは従来どおり `localhost` を維持している。

旧サーバーの終了、新サーバーの起動、停止、再起動、PID一致、配信元一致、キャッシュ制御ヘッダー、HTML内のバージョン付きアセットURL、ロビーの11ゲーム表示まで確認した。

## 現在収録しているゲーム

| ゲーム | パス | 主な内容 |
| --- | --- | --- |
| ひとりババ抜き | `games/old-maid/` | CPU戦、ペア自動処理、毎ターンの実データシャッフル |
| スライドパズル | `games/slide-puzzle/` | 3×3～5×5、手数・タイマー、PAUSE、HINT |
| 絵合わせポーカー | `games/picture-poker/` | CPU戦、変更対象、交換、SHOW DOWN、BET、配当、勝敗判定 |
| テトリス | `games/tetris/` | NEXT 3個、HOLD、ゴースト、レベル・ライン・スコア |
| 本格ポーカー | `games/draw-poker/` | 52枚、CPU戦、交換、BET、FOLD、SHOW DOWN、チップ |
| ポケットインベーダー | `games/invaders/` | 編隊、敵弾、残機、ウェーブ、ハイスコア |
| ブロック崩し | `games/breakout/` | パドル、反射角、残機、ステージ、ハイスコア |
| マインスイーパー | `games/minesweeper/` | 初手安全、連鎖展開、フラグ、2サイズ |
| ポケットスネーク | `games/snake/` | WALL・WRAP、BOOST、レベル、ハイスコア |
| ポケットランナー | `games/runner/` | 自動横スクロール、ジャンプ、速度上昇、ハイスコア |
| ビートパレード | `games/rhythm/` | 矢印・A/Bノーツ、4段階評価、コンボ、ベストスコア |

## 共通操作

| 操作 | 画面・タッチ | キーボード |
| --- | --- | --- |
| 移動・選択 | 十字キー、対象の直接タップ | 矢印キー |
| A | Aボタン、対象のタップ | Enter、Z |
| B | Bボタン | Space、Escape、X |
| SELECT | SELECTボタン | Q |
| START | STARTボタン | S |

ゲーム固有の操作説明では、タッチ操作とボタン操作を必ず併記する。

## ビルドと検証

- JavaScriptファイルは `node --check` で構文確認した。
- `node scripts/build.mjs` で `dist/` を生成できることを確認した。
- ロビーから11ゲームへアクセスできる構成にした。
- 絵合わせポーカーで双方への配札、変更対象、CPU交換、SHOW DOWN、並べ替え、勝敗判定を確認した。
- テトリスで盤面、HOLD、NEXT 3個、ゴースト、スコア表示、ポーズメニューが表示されることを確認した。
- スマートフォン相当の狭い画面で、ゲーム画面と基本操作ボタンが表示範囲へ収まるよう調整した。
- GitHub Pages等のWeb公開は行っていない。

## 今後ゲームを追加するときの方針

1. `games/<game-id>/` にゲーム一式を追加する。
2. `assets/js/games.js` のカタログへ登録する。
3. タップ操作と十字・ボタン操作の両方を実装し、説明にも併記する。
4. 共通コントローラーの基本配置を維持する。
5. 中央補助ボタンはSELECT・STARTの2つだけにする。
6. 効果音は共通サウンドプールへ追加して参照する。
7. 動作の結果を認識できる速度、待ち時間、アニメーションを設ける。
8. モバイル表示、キーボード操作、音声ON/OFF、`prefers-reduced-motion` を確認する。
9. 必要に応じて `node scripts/build.mjs` を実行し、`dist/` を更新する。

## ローカル実行

専用BATや常駐サーバーは置かず、必要なときだけプロジェクトルートを任意の静的サーバーで配信する。

```sh
python -m http.server 4173
```

ブラウザで `http://localhost:4173/` を開く。サーバーを使わず、ルートの `index.html` を直接開いても基本機能は動作する。

### 15. GitHub移行準備と操作案内の再監査

プロジェクト専用の新規GitHubリポジトリへ移行する方針へ変更した。安全を優先し、リポジトリは非公開、共同編集者は追加しない状態を初期設定とする。標準のGitHub Pagesはサイト自体がインターネットへ公開されるため、「URLを知る人だけ」という限定公開の代わりにはならない。公開範囲を了承するまではPagesを有効化しない。

GitHub管理へ切り替えるため、`localhost-start.bat`、`localhost-stop.bat`、専用Pythonサーバー、起動・終了PowerShell、PIDファイルを削除した。ローカル確認が必要な場合は任意の静的サーバーを都度起動する。

残存確認では11ゲームのソース構成と操作案内を再点検した。ロビー、ポケットランナー、ビートパレード、テトリス、本格ポーカーで不足していた「画面タップ」と「十字・A/B・キーボード」の併記を補った。ランナー、ビートパレード、本格ポーカーの画面内ボタンには `type="button"` と操作グループのアクセシビリティ情報も追加した。

### 16. 新規GitHubリポジトリへの移行

既存リポジトリを流用せず、`yr-develop/pocket-playroom` を新規作成した。リポジトリはPrivate、共同編集者0人の状態で、ローカルの `main` ブランチと接続して初回コミットをpushした。Git Credential ManagerへGitHub認証を保存し、今後は通常の `git push` で更新できる。

既存リポジトリも確認した。`yr-develop.github.io` はPublicだが共同編集者0人で、他者は閲覧できても直接編集できない。`quank` はPrivateかつ共同編集者0人で、他者は閲覧・編集ともできない。

新規リポジトリのPages設定では「Upgrade or make this repository public to enable Pages」と表示され、現在のプランではPrivateのままGitHub Pagesを有効化できないことを確認した。サイトを公開するにはリポジトリをPublicへ変更する必要があり、その場合はGitHub上のソースも閲覧可能になる。公開されたHTML・CSS・JavaScriptはブラウザへ配信されるため、Pagesの方式にかかわらず完全な秘匿はできない。

### 17. GitHub Pages公開

利用者の了承後、`yr-develop/pocket-playroom` をPublicへ変更した。共同編集者は追加していないため、第三者はソースの閲覧・フォーク・変更提案はできるが、元リポジトリへ直接pushしたり内容を削除したりはできない。

GitHub Pagesの公開元を `main` ブランチの `/ (root)` に設定した。初回の `pages-build-deployment` が成功し、`https://yr-develop.github.io/pocket-playroom/` でロビーが表示されることを確認した。公開サイト上で11ゲームの一覧、共通コントローラー、`games/old-maid/` の画面とJOKER表示まで確認した。
